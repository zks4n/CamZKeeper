<Window x:Class="CamZKeeper.Desktop.SupportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="/Assets/icon_webcam.ico"
        Title="{DynamicResource SupportWindowTitle}"
        Width="320"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="15"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="15"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock
            Grid.Row="0"
            FontSize="16"
            FontWeight="Bold"
            TextWrapping="Wrap"
            Text="{DynamicResource SupportWindow_Header}"/>

		<TextBlock
            Grid.Row="2"
            TextWrapping="Wrap"
            Text="{DynamicResource SupportWindow_Body}"/>

		<StackPanel Grid.Row="4">
			<Button
                x:Name="TwitchButton"
                Height="26"
                Margin="0,0,0,10"
                Content="{DynamicResource SupportWindow_TwitchButton}"
                Click="TwitchButton_Click"/>

			<Button
                x:Name="LivePixButton"
                Height="26"
                Content="{DynamicResource SupportWindow_LivePixButton}"
                Click="LivePixButton_Click"/>
		</StackPanel>
	</Grid>
</Window>