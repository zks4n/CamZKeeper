<Window x:Class="CamZKeeper.Desktop.ReportProblemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="/Assets/icon_webcam.ico"
        Title="{DynamicResource ReportWindowTitle}"
        Width="360"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="16"
            FontWeight="Bold"
            TextWrapping="Wrap"
            Text="{DynamicResource ReportWindow_Header}"/>

        <TextBlock
            Grid.Row="2"
            Text="{DynamicResource ReportWindow_ModelLabel}"/>

        <TextBox
            x:Name="CameraModelTextBox"
            Grid.Row="4"
            Height="26"/>

        <TextBlock
            Grid.Row="6"
            Text="{DynamicResource ReportWindow_DescriptionLabel}"/>

        <TextBox
            x:Name="DescriptionTextBox"
            Grid.Row="8"
            Height="100"
            TextWrapping="Wrap"
            AcceptsReturn="True"
            VerticalScrollBarVisibility="Auto"/>

        <TextBlock
            x:Name="FeedbackText"
            Grid.Row="10"
            TextWrapping="Wrap"/>

        <StackPanel
            Grid.Row="12"
            Orientation="Horizontal"
            HorizontalAlignment="Right">

            <Button
                x:Name="SendButton"
                Width="100"
                Height="28"
                Margin="0,0,10,0"
                Content="{DynamicResource ReportWindow_SendButton}"
                Click="SendButton_Click"/>

            <Button
                x:Name="CancelButton"
                Width="100"
                Height="28"
                Content="{DynamicResource ReportWindow_CancelButton}"
                Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>